# SQL Editor

**SQL Editor** – это легковесное кроссплатформенное приложение с графическим интерфейсом (GUI) для управления базами данных SQLite. Приложение разработано на языке Python с использованием фреймворка PyQt6.

Проект создан в рамках курсовой работы как инструмент для визуализации данных, выполнения SQL-запросов и экспорта результатов без необходимости использования консольных утилит.

## 🚀 Возможности

* **Управление базами данных**:
    * Создание новых файлов баз данных (`.db`, `.sqlite`).
    * Подключение к существующим базам.
    * Просмотр структуры таблиц в боковой панели.
* **Редактор SQL**:
    * **Подсветка синтаксиса**: цветовое выделение ключевых слов, строк, чисел и комментариев.
    * **Автодополнение**: всплывающие подсказки для ключевых слов SQL (вызов через `Ctrl+Space` или автоматически при вводе).
    * **Горячие клавиши**: `Enter` для выполнения запроса, `Shift+Enter` для переноса строки.
* **Работа с данными**:
    * Отображение результатов выборки в табличном виде.
    * Уведомления об успешном выполнении операций (`INSERT`, `UPDATE`, `CREATE`).
* **Экспорт**:
    * Сохранение результатов запроса в форматы **CSV** и **JSON**.
* **Интерфейс**:
    * Поддержка **Темной** и **Светлой** тем оформления.
    * Адаптивный дизайн с изменяемыми размерами панелей (`QSplitter`).

## 🛠 Технологический стек

* **Язык**: Python 3.10+
* **Интерфейс**: PyQt6
* **СУБД**: SQLite3 (встроенная)
* **Тестирование**: pytest, pytest-qt

## 📦 Установка и запуск

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/waitefy/SQL-Editor.git](https://github.com/waitefy/SQL-Editor.git)
    cd SQL-Editor
    ```

2.  **Создайте и активируйте виртуальное окружение (рекомендуется):**
    ```bash
    # Windows
    python -m venv .venv
    .venv\Scripts\activate

    # Linux/macOS
    python3 -m venv .venv
    source .venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Запустите приложение:**
    ```bash
    python main.py
    ```

## 📂 Структура проекта

```text
SQL-Editor/
├── main.py                 # Точка входа в приложение
├── requirements.txt        # Список зависимостей
├── sql_editor/             # Основной пакет
│   ├── db/                 # Логика работы с БД
│   │   └── connection.py   # Класс DatabaseManager
│   ├── ui/                 # Графический интерфейс
│   │   ├── main_window.py  # Главное окно
│   │   ├── editor.py       # Виджет редактора с автодополнением
│   │   ├── syntax.py       # Подсветка синтаксиса
│   │   └── styles.py       # CSS-стили (Dark/Light themes)
│   └── utils/              # Утилиты
│       └── export.py       # Экспорт в CSV/JSON
└── README.md
```

## 📄 Лицензия
Этот проект распространяется под лицензией MIT. Подробности см. в файле LICENSE.